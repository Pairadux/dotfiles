#!/bin/bash
#
# clipboard picker — select from clipboard history
#
# TODO: text-only: clipman pick --tool=rofi
# TODO: image-only: needs a solution for storing/previewing image clipboard entries

notify-send "Picker" "Clipboard picker not yet implemented"
