#compdef tldr

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _tldr (e.g. ~/.local/share/zsh_completion/_tldr).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_tldr_commands() {
  local _commands=(

  )
  _describe 'tldr commands' _commands
}

_shtab_tldr_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "(- : *)"{-v,--version}"[show program\'s version number and exit]"
  "--search[Search for a specific command from a query]:"KEYWORDS":"
  {-u,--update,--update_cache}"[Update the local cache of pages and exit]"
  {-k,--clear-cache}"[Delete the local cache of pages and exit]"
  {-p,--platform}"[Override the operating system \[android, freebsd, linux, netbsd, openbsd, osx, sunos, windows, common\]]:PLATFORM:(android freebsd linux netbsd openbsd osx sunos windows common)"
  {-l,--list}"[List all available commands for operating system]"
  {-s,--source}"[Override the default page source]:source:"
  {-c,--color}"[Override color stripping]"
  {-r,--render}"[Render local markdown files]"
  {-L,--language}"[Override the default language]:language:"
  {-m,--markdown}"[Just print the plain page file.]"
  "--short-options[Display shortform options over longform]"
  "--long-options[Display longform options over shortform]"
  "(- : *)--print-completion[print shell completion script]:print_completion:(bash zsh tcsh fish powershell)"
  "(*)::command to lookup:shtab_tldr_cmd_list"
)

# guard to ensure default positional specs are added only once per session
_shtab_tldr_defaults_added=0


_shtab_tldr() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->tldr'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_tldr_defaults_added )); then
    if (( ${_shtab_tldr_options[(I)${(q)one_or_more}*]} +          ${_shtab_tldr_options[(I)${(q)remainder}*]} +          ${_shtab_tldr_options[(I)${(q)default}]} == 0 )); then
      _shtab_tldr_options+=(': :_shtab_tldr_commands' '*::: :->tldr')
    fi
    _shtab_tldr_defaults_added=1
  fi
  _arguments -C -s $_shtab_tldr_options

  case $state in
    tldr)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_tldr-$line[1]:"
      case $line[1] in

      esac
  esac
}

# Custom Preamble
shtab_tldr_cmd_list(){
          _describe 'command' "($("/usr/bin/python" -m tldr --list))"
        }
# End Custom Preamble


typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_tldr -N tldr
else
  # autoload from fpath, call function directly
  _shtab_tldr "$@"
fi
